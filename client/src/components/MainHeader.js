// Components
import React from "react";

// Styled Components
import styled from "styled-components";

const HeaderStyle = styled.h1`
  text-align: center;
  margin-top: 20px;
`;

const MainHeader = () => {
  return <HeaderStyle>ITHR Form</HeaderStyle>;
};

export default MainHeader;
